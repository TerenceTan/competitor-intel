---
phase: 02.1-per-market-competitor-curation-operator-driven-show-hide
verified: 2026-05-14T13:00:00Z
status: human_needed
score: 12/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Visit /markets/sg with an empty competitor_markets table and confirm the Digital Presence section renders identically to Phase 2 (all competitors visible, no filtering)"
    expected: "All competitors with social data appear; no 'Emerging Competitors' rail if logs/serp_research_sg.csv is absent"
    why_human: "Default-safe contract (D2.1-04/05) is structurally verified in code (hasCuration ternary) but the full render path requires a live DB and running Next.js server"
  - test: "Visit /admin/competitors/pepperstone — confirm 8 rows render with 'Not curated' state; save one row as 'active'; visit /markets/sg and confirm only that competitor appears in Digital Presence"
    expected: "Save succeeds (toast), page revalidates, /markets/sg narrows to only the active competitor(s); revalidatePath fired for /markets/sg"
    why_human: "End-to-end write path (upsertCompetitorMarket → competitor_markets DB write → revalidatePath → /markets/sg re-render) requires a live server + DB"
  - test: "Visit /admin/data-health and confirm the 'Per-market curation' tile appears between the Apify Cost tile and the Scrapers table, showing '0 of 8 markets seeded' when the table is empty"
    expected: "Tile renders; seededMarkets count is 0 with fallback copy visible; tile is additive (Phase 2 zero-result breakdown table still present)"
    why_human: "JSX position and tile rendering requires live browser; confirmed structurally but visual placement and text require human"
  - test: "Run 'python3 scrapers/admin/import_market_decisions.py --help' on EC2 and confirm --input, --purge, --dry-run are listed"
    expected: "Help text shows all three flags; script exits 0"
    why_human: "EC2 environment gate — verifiable only on the production instance (local worktree may lack the .venv/openpyxl setup)"
---

# Phase 02.1: Per-Market Competitor Curation Verification Report

**Phase Goal:** Marketing managers see only the brokers that are actually competing in their market — operator-curated per-market SHOW/HIDE via a new `competitor_markets` table, with default-safe fallback to Phase 2 behavior (show all) when the table is empty.
**Verified:** 2026-05-14T13:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `competitor_markets(competitor_id, market_code, status, notes, updated_at)` table exists with composite PK, FK on competitor_id, CHECK on status in ('active','planned','withdrawn','emerging') | ✓ VERIFIED | `scrapers/db_utils.py` lines 209-218: `CREATE TABLE IF NOT EXISTS competitor_markets` with matching DDL; `src/db/schema.ts` lines 200-215: `competitorMarkets` export with `primaryKey({columns:[t.competitorId,t.marketCode]})` and `.references(()=>competitors.id)` |
| 2 | `/markets/<code>` default view filters to `competitor_markets.status='active'`; non-active and uncurated competitors hidden | ✓ VERIFIED | `src/app/(dashboard)/markets/[code]/page.tsx` line 437: `filter((r) => r.status === "active")`; line 440: `hasCuration = curatedMarketRows.length > 0`; lines 441-443: `filteredSocialRows = hasCuration ? socialRows.filter(...active...) : socialRows`. Lines 810-841: JSX uses `filteredSocialRows` throughout Digital Presence section. |
| 3 | When `competitor_markets` is EMPTY for a market, `/markets/<code>` behavior is IDENTICAL to Phase 2 (show all) — default-safe contract D2.1-04/D2.1-05 | ✓ VERIFIED | `hasCuration = curatedMarketRows.length > 0` (line 440); when false, `filteredSocialRows = socialRows` (line 443). No filter applied. The Phase 2 resolver is unchanged; the filter is additive on top. |
| 4 | "Emerging Competitors" rail renders below Digital Presence showing STRONG-SERP competitors (queries_appeared >= 2 AND best_rank <= 10) not yet curated in any status | ✓ VERIFIED | `readEmergingSerpRows` helper (lines 159-189): parses `logs/serp_research_<market>.csv`, applies `qa >= 2 && br <= 10` threshold. `curatedAnyIds` (line 567) dedupes against ALL competitor_markets rows. `emergingRail` rendered at lines 896-941 only when `emergingRail.length > 0`. |
| 5 | Missing SERP CSV is a graceful no-op — Emerging rail is omitted with no error | ✓ VERIFIED | `readEmergingSerpRows` lines 163-165: `try { raw = await readFile(...) } catch { return []; // file not present — graceful no-op }`. Rail wrapped in `{emergingRail.length > 0 && (...)}`  — no empty header rendered. |
| 6 | `/admin/competitors/<id>` lets operator edit per-market status without redeploys | ✓ VERIFIED | `src/app/(dashboard)/admin/competitors/[id]/page.tsx` exists; `force-dynamic`; reads `competitor_markets` for all existing rows; passes to `PerMarketStatusTable`. `upsertCompetitorMarket` and `clearCompetitorMarket` server actions in `actions.ts` with `revalidatePath` fan-out. |
| 7 | Admin UI renders all 8 APAC markets as rows — even if no competitor_markets row exists for some (shows 'Not curated') | ✓ VERIFIED | `src/components/admin/per-market-status-table.tsx` lines 51-64: iterates `PRIORITY_MARKETS.map(...)`, falling back to `status: ""` for markets not found in `existingRows`. Dropdown default is `— Not curated —`. |
| 8 | Both server actions validate competitorId (FK check), marketCode (PRIORITY_MARKETS), status (4-value enum); write change_events audit row; call revalidatePath | ✓ VERIFIED | `actions.ts` lines 363-490: `isCuratedMarketCode` + `isCuratedStatus` validators; FK pre-flight via `db.select...competitors`; audit rows with `fieldName: "market_curation_edit"`; `revalidatePath` for `/admin/competitors/${id}`, `/markets/${marketCode}`, `/admin/data-health`. |
| 9 | `scrapers/admin/import_market_decisions.py` operator import script exists with INSERT OR REPLACE upsert, --purge, --dry-run, audit row to change_events | ✓ VERIFIED | File exists (309 lines). INSERT OR REPLACE at line 266; `--purge` DELETE loop at lines 256-261; `--dry-run` branch at lines 243-250; change_events audit at lines 275-289; `contextlib.closing` at line 254; openpyxl ImportError at lines 52-57. |
| 10 | Re-running import is idempotent (INSERT OR REPLACE on composite PK) | ✓ VERIFIED | `INSERT OR REPLACE INTO competitor_markets (competitor_id, market_code, ...)` — composite PK from Plan 02.1-01 makes duplicate (competitor_id, market_code) pairs idempotent. Verified structurally; end-to-end smoke against temp DB documented in SUMMARY. |
| 11 | `/admin/data-health` shows "Per-market curation: N of 8 markets seeded" tile (D2.1-12) | ✓ VERIFIED | `data-health/page.tsx` lines 51, 112-117: 5th parallel query `SELECT marketCode FROM competitor_markets GROUP BY marketCode`; line 138: `seededMarkets = seededRows.length`; lines 175-197: tile JSX with "of 8 markets seeded" copy between Apify Cost and Scrapers table. |
| 12 | Phase 1 `<EmptyState reason='scraper-failed'>` cell-level indicators and Phase 2 resolver are not modified — Phase 2.1 filter is additive | ✓ VERIFIED | Phase 2 `socialRows` const preserved at line 396 (construction unchanged); filter runs AFTER `.sort()` at line 441; `EmptyState` imports and usage are unchanged in the Digital Presence section. CONTEXT.md D2.1-11 contract upheld. |

**Score:** 12/12 truths verified

### Deferred Items

None. All Phase 2.1 items are implemented.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/db/schema.ts` | `competitorMarkets` Drizzle export with composite PK | ✓ VERIFIED | Lines 200-215; `primaryKey({columns:[t.competitorId,t.marketCode]})`, FK, 5 columns match spec |
| `scrapers/db_utils.py` | `CREATE TABLE IF NOT EXISTS competitor_markets` with CHECK | ✓ VERIFIED | Lines 209-223; CHECK on status IN('active','planned','withdrawn','emerging'), composite PK, index on (market_code,status) |
| `scrapers/admin/import_market_decisions.py` | Operator seed-import script | ✓ VERIFIED | 309 lines; all required logic present |
| `src/app/(dashboard)/markets/[code]/page.tsx` | Curation filter + Emerging rail | ✓ VERIFIED | `curatedMarketRows` 11th query, `hasCuration`/`filteredSocialRows` filter, `readEmergingSerpRows`, `emergingRail` JSX |
| `src/app/(dashboard)/admin/competitors/[id]/page.tsx` | Admin per-market status editor page | ✓ VERIFIED | Exists at correct path; `force-dynamic`; parallel queries; passes existingRows to PerMarketStatusTable |
| `src/components/admin/per-market-status-table.tsx` | Client component with 8-row table | ✓ VERIFIED | `use client`; iterates PRIORITY_MARKETS; per-row dirty/saving state; calls both server actions; Sonner toasts |
| `src/app/(dashboard)/admin/actions.ts` | Server actions upsertCompetitorMarket + clearCompetitorMarket | ✓ VERIFIED | Lines 363-490; both exported; validation; audit rows; revalidatePath |
| `src/app/(dashboard)/admin/data-health/page.tsx` | "Markets seeded" tile | ✓ VERIFIED | 5th parallel query; `seededMarkets` derivation; tile JSX between Apify Cost and Scrapers table |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `scrapers/db_utils.py get_db()` | `competitor_markets` table | `CREATE TABLE IF NOT EXISTS` on every connection | ✓ WIRED | Lines 209-218 in db_utils.py; idempotent migration block |
| `src/db/schema.ts competitorMarkets` | `competitor_markets` table | Drizzle ORM type-safe interface | ✓ WIRED | Lines 200-215; composite PK matches DDL |
| `import_market_decisions.py` | `competitor_markets` + `change_events` | `INSERT OR REPLACE` + audit INSERT via `contextlib.closing(get_db())` | ✓ WIRED | Lines 254-290; both tables written in single transaction |
| `/markets/[code] page.tsx` | `competitor_markets` (read) | Drizzle `select().from(competitorMarkets).where(eq(marketCode,...))` | ✓ WIRED | Line 295-303 in Promise.all; `curatedMarketRows` destructured at line 230 |
| `/markets/[code] page.tsx` | `filteredSocialRows` JSX | `hasCuration` ternary narrows `socialRows` to `curatedActiveIds` set | ✓ WIRED | Lines 440-443; JSX refs at 810, 812, 817, 835, 841 |
| `/markets/[code] page.tsx` Emerging rail | `logs/serp_research_<market>.csv` | `readEmergingSerpRows` → `node:fs/promises readFile` | ✓ WIRED | Lines 159-189; ENOENT caught → returns [] |
| `PerMarketStatusTable` | `upsertCompetitorMarket` / `clearCompetitorMarket` | Direct server action import + invocation on button click | ✓ WIRED | `per-market-status-table.tsx` lines 14-16 import; lines 79, 86-91 invocations |
| `upsertCompetitorMarket` | `competitor_markets` (write) | Drizzle `insert().onConflictDoUpdate()` on composite PK | ✓ WIRED | `actions.ts` lines 402-418 |
| `upsertCompetitorMarket` | `change_events` (audit) | Drizzle `insert().values({fieldName:'market_curation_edit',...})` | ✓ WIRED | `actions.ts` lines 422-431 |
| `data-health/page.tsx` | `competitor_markets` (count) | Drizzle `select().from(competitorMarkets).groupBy(marketCode)` | ✓ WIRED | Lines 112-117; `seededMarkets = seededRows.length` at line 138 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `/markets/[code]/page.tsx` Digital Presence | `filteredSocialRows` | `curatedMarketRows` from Drizzle query on `competitor_markets` WHERE `marketCode = current` | Yes — live Drizzle query; empty table = Phase 2 behavior | ✓ FLOWING |
| `/markets/[code]/page.tsx` Emerging rail | `emergingRail` | `readEmergingSerpRows` → `logs/serp_research_<market>.csv` fs read | Yes — reads operator-side CSV; graceful [] on missing file | ✓ FLOWING |
| `data-health/page.tsx` seeded tile | `seededMarkets` | Drizzle `SELECT marketCode FROM competitor_markets GROUP BY marketCode` | Yes — live count; returns 0 when table empty | ✓ FLOWING |
| `per-market-status-table.tsx` | `rows` state | `existingRows` prop hydrated from server page's Drizzle query | Yes — server pre-fetches `competitor_markets` rows for the competitor | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| import_market_decisions.py compiles | `python3 -m py_compile scrapers/admin/import_market_decisions.py` | Structural verification confirmed via file read; SUMMARY documents exit 0 | ✓ PASS |
| schema.ts exports competitorMarkets | `grep -c "export const competitorMarkets" src/db/schema.ts` | 1 | ✓ PASS |
| db_utils.py contains migration block | `grep -c "CREATE TABLE IF NOT EXISTS competitor_markets" scrapers/db_utils.py` | 1 (verified by direct read at lines 209-218) | ✓ PASS |
| admin route file exists | `test -f src/app/(dashboard)/admin/competitors/[id]/page.tsx` | File confirmed present | ✓ PASS |
| per-market-status-table.tsx exists | `test -f src/components/admin/per-market-status-table.tsx` | File confirmed present, 203 lines | ✓ PASS |
| hasCuration default-safe contract | `grep -c "hasCuration" markets/[code]/page.tsx` | 2 occurrences — declaration + ternary | ✓ PASS |
| End-to-end revalidatePath fan-out | `grep -c "revalidatePath.*markets" actions.ts` | 2 (upsert + clear both call `revalidatePath('/markets/${marketCode}')`) | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|-------------|----------------|-------------|--------|----------|
| MARKET-02 | 02.1-01, 02.1-02, 02.1-03, 02.1-04 | Per-market UI narrowed to operator-curated competitor list | ✓ SATISFIED | Schema + import + filter + admin all land; filteredSocialRows wired to competitor_markets status='active' |
| MARKET-03 | 02.1-01, 02.1-02, 02.1-03, 02.1-04 | Presence-aware fallback: empty table = Phase 2 behavior | ✓ SATISFIED | `hasCuration` ternary at line 440-443; empty competitor_markets for a market = `filteredSocialRows = socialRows` (no filter) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No placeholder returns, no hardcoded empty data, no TODO/FIXME, no stub implementations found in any of the 6 modified/created files.

### Human Verification Required

**Note:** All automated checks pass. The following 4 items require a running Next.js server + production DB to confirm end-to-end behavior.

#### 1. Default-Safe Contract — Empty Table Smoke

**Test:** With `competitor_markets` empty for all markets, visit `/markets/sg` (or any APAC v1 market) and compare the Digital Presence section against what Phase 2 renders.
**Expected:** Identical output — all competitors with social data are visible; no "Emerging Competitors" rail (unless `logs/serp_research_sg.csv` exists with qualifying rows).
**Why human:** The `hasCuration` ternary is structurally correct in code, but a running server is needed to confirm no regressions were introduced in the surrounding Digital Presence render path.

#### 2. Write Path — upsert activates curation

**Test:** Navigate to `/admin/competitors/pepperstone`, set Singapore status to `active`, click Save. Then visit `/markets/sg`.
**Expected:** Save shows success toast. `/markets/sg` Digital Presence section narrows to show only Pepperstone (and any other competitor explicitly set to `active` for sg). revalidatePath causes the change to reflect without a manual reload.
**Why human:** Full write path (client action → server action → DB write → revalidatePath → page re-render) requires live server with DB access.

#### 3. Data Health Tile — visual placement

**Test:** Visit `/admin/data-health` and verify the "Per-market curation" tile appears BETWEEN the Apify Cost tile and the Scrapers table; shows "0 of 8 markets seeded" (or the current seeded count).
**Expected:** Tile is visible, shows correct count, helper copy is present when count < 8.
**Why human:** JSX section placement and visual rendering require live browser.

#### 4. EC2 CLI gate for import script

**Test:** On EC2, run `.venv/bin/python scrapers/admin/import_market_decisions.py --help`.
**Expected:** Help text lists `--input`, `--purge`, `--dry-run`; exits 0.
**Why human:** Confirms openpyxl is installable and the script's path resolution (`_PROJECT_ROOT` detection) is correct on the production EC2 environment (which uses `/home/ubuntu/app/` root, not a local dev path).

### Gaps Summary

No gaps. All 12 must-have truths are VERIFIED. All 5 ROADMAP Phase 2.1 success criteria are met. All 4 plans shipped with substantive implementations — no stubs, no orphaned files, all key links wired end-to-end.

**ROADMAP SC#1 enum note:** ROADMAP.md states `status ∈ {active, planned, withdrawn}` (3 values), but the implementation shipped 4 values: `{active, planned, withdrawn, emerging}`. This is an intentional expansion documented in the PLAN-INDEX, CONTEXT.md D2.1-01, and all 4 plan must_haves — `emerging` enables the Emerging rail's dedupe-against-all-statuses contract and lets operators mark a SERP-watched broker directly from the admin UI. The 3 ROADMAP values are a strict subset of the implemented 4. This is not a defect; it is a coherent design choice documented in the planning artifacts. The ROADMAP success criteria text was not updated to reflect the `emerging` addition — a minor documentation gap but not blocking.

---

_Verified: 2026-05-14T13:00:00Z_
_Verifier: Claude (gsd-verifier)_
